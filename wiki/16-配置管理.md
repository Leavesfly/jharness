# 16 · 配置管理

> 包路径：`io.leavesfly.jharness.config`
>
> 核心类：`Settings`（POJO + load/save） + `SettingsBootstrap`（默认资源 seed）

---

## 1. 配置加载优先级（高优先级覆盖低优先级）

```
1. 命令行参数 (CLI Option, 最高优先级)
2. 环境变量 (OPENAI_API_KEY 等)
3. 项目配置 (<cwd>/.jharness/settings.json)
4. 用户配置 (~/.jharness/settings.json)
5. classpath 默认 (defaults/settings.json, 最低优先级)
```

`Settings.load()` 在内部按从低到高顺序逐层 merge。

---

## 2. 默认值速查

| 字段 | 默认值 | 说明 |
|------|--------|------|
| `model` | `"qwen3.5:4b"` | 默认模型（本地 Ollama） |
| `apiKey` | `null`（本地端点用占位 `"ollama"`） | API 凭据 |
| `baseUrl` | `"http://localhost:11434/v1"` | OpenAI 兼容端点 |
| `maxTokens` | `4096` | 单次响应上限 |
| `permissionMode` | `DEFAULT` | 权限模式 |
| `maxTurns` | `8` | ReAct 循环上限 |
| `effort` | `"medium"` | 思考强度（low/medium/high） |
| `passes` | `1` | 多轮 pass 次数 |
| `theme` | `"default"` | UI 主题 |
| `vimEnabled` | `false` | Vim 输入模式 |
| `voiceEnabled` | `false` | 语音模式 |
| `fastMode` | `false` | 快速响应模式 |
| `outputStyle` | `"default"` | 输出风格 |
| `provider` | `"openai"` | LLM Provider |
| `dailyBudgetUsd` | `0` (不限制) | 每日预算 USD |
| `connectTimeoutSeconds` | `30` | OkHttp 连接超时 |
| `readTimeoutSeconds` | `300` | OkHttp 读超时 |
| `writeTimeoutSeconds` | `30` | OkHttp 写超时 |
| `messageCompactionTokenBudget` | `0` (用 32 000) | 压缩 token 预算 |
| `messageCompactionMaxMessages` | `0` (用 20) | 压缩条数阈值 |
| `autoSaveSessions` | `true` | 自动保存会话 |
| `allowedTools` | `[]` | 工具白名单 |
| `deniedTools` | `[]` | 工具黑名单 |
| `pathRules` | `[]` | 路径规则 |
| `deniedCommandPatterns` | `[]` | 命令黑名单 glob |
| `enabledPlugins` | `{}` | 插件开关 |
| `mcpServers` | `{}` | MCP 服务器配置 |

---

## 3. 配置文件示例

`~/.jharness/settings.json`（完整示例）：

```json
{
  "model": "deepseek-chat",
  "baseUrl": "https://api.deepseek.com/v1",
  "maxTokens": 8192,
  "maxTurns": 12,
  "permissionMode": "DEFAULT",

  "effort": "medium",
  "passes": 1,
  "theme": "dark",
  "vimEnabled": false,
  "fastMode": false,
  "outputStyle": "default",

  "dailyBudgetUsd": 5.0,
  "connectTimeoutSeconds": 30,
  "readTimeoutSeconds": 300,
  "writeTimeoutSeconds": 30,

  "messageCompactionTokenBudget": 32000,
  "messageCompactionMaxMessages": 20,
  "autoSaveSessions": true,

  "allowedTools": ["read_file", "glob", "grep", "lsp"],
  "deniedTools": [],
  "pathRules": [
    { "pattern": "**/.git/**", "allow": false },
    { "pattern": "**/.env*",   "allow": false },
    { "pattern": "src/**",      "allow": true }
  ],
  "deniedCommandPatterns": [
    "rm -rf /*",
    "sudo *",
    "curl * | sh"
  ],

  "enabledPlugins": {
    "my-plugin": true,
    "another-plugin": false
  },

  "mcpServers": {
    "filesystem": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "/tmp"]
    }
  }
}
```

**API Key 不写入文件**：`Settings.apiKey` 字段用 `@JsonIgnore` 标记，避免 `toJson` / `save` 时泄漏到磁盘。Key 只能通过环境变量或 `/login` 命令（写入独立的凭据存储）提供。

---

## 4. 环境变量速查

| 环境变量 | 等价配置 |
|---------|---------|
| `OPENAI_API_KEY` / `ANTHROPIC_API_KEY` | `apiKey` |
| `OPENAI_BASE_URL` / `ANTHROPIC_BASE_URL` | `baseUrl` |
| `JHARNESS_MODEL` / `OPENAI_MODEL` | `model` |
| `JHARNESS_MAX_TOKENS` | `maxTokens` |
| `JHARNESS_HOOK_DEPTH` | Hook 递归深度计数器（内部用） |

加载逻辑在 `Settings.loadFromEnvironment()`（构造器调用），格式错误（如 `JHARNESS_MAX_TOKENS=abc`）会降级到默认值，不阻断启动。

---

## 5. 默认资源 seed（`SettingsBootstrap`）

首次启动且 `~/.jharness/settings.json` 不存在时，从 classpath 释放默认资源到用户主目录：

| classpath 资源 | 目标路径 |
|---------------|---------|
| `defaults/settings.json` | `~/.jharness/settings.json` |
| `defaults/plugins/builtin-defaults/` | `~/.jharness/plugins/builtin-defaults/` |

### 5.1 设计要点

1. **幂等**：仅在目标不存在时拷贝，绝不覆盖用户修改
2. **失败不阻断**：任何 IO 异常降级为 warn 日志，主流程继续
3. **兼容两种部署**：同时支持 classpath 为 `file:` 目录（IDE 调试）和 `jar:` 归档（生产 JAR）

### 5.2 入口

```java
SettingsBootstrap.seedIfAbsent();              // 用默认 ~/.jharness
SettingsBootstrap.seedIfAbsent(customDir);     // 测试友好
```

`JHarnessApplication.call()` 在 `Settings.load()` 之前调用一次。

---

## 6. 关键 API

### 6.1 读 / 写

```java
Settings s = Settings.load();          // 多层加载
s.setModel("gpt-4o");
s.setDailyBudgetUsd(new BigDecimal("10.0"));
s.save();                              // 写回 ~/.jharness/settings.json（不含 apiKey）

String json = s.toJson();              // 序列化为 JSON 字符串
```

### 6.2 动态 get / set

```java
String value = s.get("model");                          // → "qwen3.5:4b"
boolean ok = s.set("permissionMode", "FULL_AUTO");      // → true
boolean ok2 = s.setPermissionMode("plan");              // 大小写不敏感
```

支持的 key：`model` / `apiKey` / `baseUrl` / `maxTokens` / `permissionMode` / `maxTurns` / `theme` / `effort` / `passes` / `fastMode` / `vimEnabled` / `voiceEnabled` / `outputStyle` 等。

### 6.3 路径常量

```java
Settings.getDefaultConfigDir()    // → ~/.jharness
Settings.getDefaultDataDir()      // → ~/.jharness/data
```

---

## 7. 子配置文件

JHarness 把不同领域的配置拆到独立文件，便于版本管理：

| 文件 | 内容 | 加载方 |
|------|------|--------|
| `~/.jharness/settings.json` | 主配置 | `Settings.load()` |
| `~/.jharness/hooks.json` | Hook 注册 | `HookRegistry` |
| `~/.jharness/mcp-servers.json` | MCP 服务器 | `McpClientManager.addServer` |
| `~/.jharness/keybindings.json` | 按键绑定 | `KeybindingLoader` |
| `~/.jharness/skills/*.md` | 用户技能 | `SkillLoader` |
| `~/.jharness/plugins/*/plugin.json` | 用户插件 | `PluginLoader` |
| `~/.jharness/data/sessions/*.json` | 会话快照 | `SessionStorage` |
| `~/.jharness/data/memories/<project>/*.json` | 跨会话记忆 | `MemoryManager` |
| `~/.jharness/data/cron_jobs.json` | 定时任务 | `CronRegistry` |
| `~/.jharness/data/tasks/<id>.log` | 后台任务输出 | `BackgroundTaskManager` |
| `~/.jharness/plugin-trust.json` | 插件信任 | `extension/plugins/trust/` |

项目级同名文件位于 `<cwd>/.jharness/` 下，优先级高于用户级。

---

## 8. 运行时配置命令

| 命令 | 行为 |
|------|------|
| `/config` | 查看所有配置（敏感字段已脱敏） |
| `/config <key>` | 查看单个值 |
| `/config set <key> <value>` | 修改 + 自动保存 |
| `/model <name>` | 快捷切换模型 |
| `/permissions <mode>` | 切换权限模式 |
| `/theme <name>` | 切换主题 |
| `/login` | 设置 API Key（写独立凭据） |
| `/logout` | 清空 API Key |
| `/init` | 在当前 cwd 创建 `.jharness/` 项目目录 |
| `/doctor` | 环境诊断（Java 版本 / Key / 端点连通性） |
| `/effort low\|medium\|high` | 思考强度 |
| `/passes <n>` | 多轮 pass 次数 |
| `/fast on\|off` | 切换快速模式 |
| `/privacy` | 数据隐私配置 |
| `/ratelimit` | 限流配置 |

---

## 9. 项目级配置

`<cwd>/.jharness/settings.json` 用于项目级覆盖。常见用法：

```json
{
  "model": "deepseek-chat",
  "permissionMode": "PLAN",
  "allowedTools": ["read_file", "glob", "grep"],
  "systemPrompt": "你是一个 Java 微服务架构专家。"
}
```

加载顺序：用户级 → 项目级（项目级 merge 覆盖用户级）。

### 9.1 系统提示词文件

除 `settings.systemPrompt` 外，还可以放独立的 `CLAUDE.md`（兼容 Claude Code 生态）或 `JHARNESS.md` 作为系统提示词文件：

```
<cwd>/CLAUDE.md
<cwd>/JHARNESS.md
~/.jharness/CLAUDE.md
```

加载顺序：`Settings.systemPrompt` > 项目 `CLAUDE.md` > 用户 `CLAUDE.md` > 内置默认。

`MessageCompactionService.withSystemPromptTokens(...)` 会自动扣除系统提示词的 token 预算。

---

## 10. 安全注意事项

1. **API Key 不写文件**：通过 `@JsonIgnore` 隔离
2. **路径规则 / 命令黑名单** 是项目级共享的安全栅栏，不要在公开仓库提交项目级 `settings.json` 时泄漏内部规则
3. **MCP 服务器配置** 中的 `headers.Authorization` 等敏感字段同样应通过环境变量注入
4. **`/login` 写的凭据** 默认放 `~/.jharness/credentials.json`，权限 0600

---

## 11. 关键类清单

| 类 | 文件 | 行数 | 职责 |
|----|------|------|------|
| `Settings` | `config/Settings.java` | 511 | 配置 POJO + load/save + getter/setter |
| `SettingsBootstrap` | `config/SettingsBootstrap.java` | 247 | 首启 seed 默认资源 |
| `ConfigCommandHandler` | `command/builtin/config/ConfigCommandHandler.java` | — | `/config` 命令族 |
| `ConfigPlusCommandHandler` | `command/builtin/config/ConfigPlusCommandHandler.java` | — | `/effort` `/passes` `/plugin` 等 |
| `KeybindingLoader` | `command/keybindings/KeybindingLoader.java` | — | 按键配置加载 |

---

## 12. 配置项与子系统对照

| 子系统 | 关键配置 |
|--------|---------|
| API 客户端 | `model` / `apiKey` / `baseUrl` / `maxTokens` / `connectTimeoutSeconds` / `readTimeoutSeconds` / `writeTimeoutSeconds` |
| 引擎 | `maxTurns` / `messageCompactionTokenBudget` / `messageCompactionMaxMessages` / `dailyBudgetUsd` |
| 权限 | `permissionMode` / `allowedTools` / `deniedTools` / `pathRules` / `deniedCommandPatterns` |
| 会话 | `autoSaveSessions` |
| UI | `theme` / `vimEnabled` / `voiceEnabled` / `outputStyle` / `effort` / `passes` / `fastMode` |
| 插件 | `enabledPlugins` |
| MCP | `mcpServers` |

---

## 13. 下一步

- 引擎装配如何读 settings → [04-核心引擎 § 2](04-核心引擎-QueryEngine.md#2-构造与装配)
- 权限配置语义 → [08-权限系统 § 9](08-权限系统.md#9-配置示例)
- MCP 配置语义 → [12-MCP协议客户端 § 3](12-MCP协议客户端.md#3-配置文件格式)
